---
title: 星璃音乐·体素联机大厅
date: '2026-09-07'
updated: '2026-09-13'
status: published
category: 本地项目
tags:
  - 联机
  - 体素
  - Flutter
  - 一起听
owner: admin
excerpt: 星璃音乐里"一起听 + 体素联机"的客户端栈：WebSocket 长连、UDP 局域网发现、星型房主拓扑、"主机即 DJ"的播放跟随。本地优先——不依赖中转服务器也能在局域网跑起来。
---

## 它是什么

这是「星璃·无限音乐画布」里负责**多人同空间**的客户端模块：一个体素世界大厅，朋友们进来后能互见、能聊天、能一起编辑场景，房主播放音乐时其他人自动跟随曲目与进度。

它和音乐播放器共享同一套状态，是"可停留的空间"从单人走向多人的一步。

## 协议栈

| 组件 | 职责 | 传输 |
|------|------|------|
| **NetNode** | 长连接收发 `NetMessage`（JSON 信封：`t` / `f` / `to` / `p`） | WebSocket |
| **LanDiscovery** | 局域网设备发现，免去手动填地址 | UDP 广播（8767） |
| **session_provider** | 星型拓扑（房主-客户端），房主即 DJ | 应用层 |
| **中转服务器** | 跨公网 / 跨 NAT 的扇出（可选） | WebSocket（8765） |

`NetMsgType` 关键索引：

| 值 | 类型 | 用途 |
|----|------|------|
| 4 | `transform` | 体素角色位置与朝向 |
| 5 | `edit` | 场景编辑增量 |
| 6 | `vitals` | 在线状态心跳 |
| 7 | `chat` | 聊天 |
| 8 | `listenState` | **一起听**（曲目 + 进度 + 播放态） |
| 9 | `requestListen` | 客户端请求同步当前播放 |
| 12 | `editSnapshot` | 场景完整快照（新人加入时用） |

> [!NOTE]
> 设计上把"联机"和"播放"彻底解耦：联机只负责把 `listenState` 这种控制帧同步过去，具体播放由各自客户端本地执行——所以断网也不会让别人的歌停下，只是不再同步。

## 两种连接方式

大厅提供开关：

- **局域网**：直接走 `LanDiscovery` beacon，零配置，不需要任何服务器
- **中转服务器**：连 `ws://<地址>:8765/ws`，跨公网 / 跨 NAT（见「星璃音乐·中转服务器」项目）

建房后自动复制 6 位房间号，对方填同地址 + 房间号即可加入。

## 加入房间的流程

新人加入不是"连上就行"，要补齐三样东西，缺一个体验就会残废：

1. **房间成员表**（谁在房里）
2. **场景完整快照** `editSnapshot`——只发增量 `edit` 的话，后进来的人看到的是半个世界
3. **当前播放态** `listenState`——否则新人的播放器停在空白，要等到下一次广播才跟上

> [!WARNING]
> 第 2 和第 3 条是最容易漏的。测试时只开两个客户端同时进房，会发现一切正常；一旦让第三个"迟到"，问题就全冒出来了。**一定要专门测"中途加入"这个路径。**

## 为什么本地优先

整个联机栈跑在用户设备上，房间状态不落任何第三方服务器（除非显式接中转）。这意味着一起听是**点对点的陪伴**，没有"云端房间"要维护，也没有数据要上传。

代价是跨网必须自己解决 NAT 问题——所以中转服务器被设计成**可选的外挂**，而不是必经的基础设施。局域网用户永远不需要知道它存在。

## 当前状态

协议栈与客户端模块已完整（含局域网发现、星型房主、一起听广播、场景编辑同步），中转服务器已可常驻运行。正在打磨的是联机稳定性：重连退避、过期状态丢弃、以及"中途加入"的完整补帧。

## 参考来源

1. Dart `dart:io` 库 — WebSocket 客户端（NetNode 基于它），https://api.dart.dev/ ，访问于 2026-09-13。
2. WebSocket — MDN Web Docs，https://developer.mozilla.org/en-US/docs/Web/API/WebSocket ，访问于 2026-09-13。
3. 用户数据报协议（UDP）— 维基百科（LanDiscovery 基于 UDP 广播做局域网发现），https://en.wikipedia.org/wiki/User_Datagram_Protocol ，访问于 2026-09-13。
