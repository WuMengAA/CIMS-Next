---
title: 多 Agent 协作开发：星璃工程的并发实践
date: '2026-09-07'
status: published
category: 工程笔记
tags:
  - 协作
  - 流程
  - 多 Agent
  - 工程
owner: admin
excerpt: 当多个 Agent 同时改同一个仓库，怎么不互相踩？记录星璃工程里"暂停协调、分面并发、回归验证"的协作纪律。
---

星璃的工程不是一个人（或一个 Agent）从头写到尾。常常出现：一个 Agent 在改集控面板，另一个在接域名，第三个在写内容。多个 Agent 并发改同一个 git 仓库，**最大的风险不是写错，而是互相覆盖提交**。

## 第一条纪律：动之前先对齐

一旦知道"有另一个 Agent 在改重要部分"，第一反应是**暂停、不抢提交**。先问清楚对方在动哪几个文件、预计改多久，再决定自己是等一等还是绕开那片区域。盲目 `commit` 极可能把对方未合并的改动挤掉，或制造冲突。

> [!WARNING]
> 绝不 `git add -A` 一把梭。精确用 `git commit -- <pathspec>` 只提交自己负责的文件，避免把别人的半成品或历史遗留（如大体积 release 目录）带进提交。

## 分面并发：各改各的，少交叉

把工作按"文件面"切开：A 改 `admin/` 与菜单，B 改 `content/` 与 RSS，C 改部署脚本。只要不碰同一批文件，就能并行。冲突概率随"面"的清晰度指数下降。

## 回访与回归

约好时间点回访（比如两小时后）：拉最新、看对方提交了什么、确认自己要补的微调是否还成立。功能改动后跑对应的回归——联机改了就跑协议/特性三层验证，构建改了就重新出包。

## 工具底座

这套纪律能成立，靠的是 git 本身的分布式特性：每个人/每个 Agent 有完整历史，提交是本地动作，合并时才需要协调。再配合明确的所有者与任务清单，并发就不乱。

## 参考来源

1. Git — 分布式版本控制（多 Agent 并行提交的基础），https://git-scm.com/ ，访问于 2026-09-07。
2. SvelteKit — 文档（星璃工程的内容平台底座），https://kit.svelte.dev ，访问于 2026-09-07。
