---
title: MD 扩展语法自检
date: '2026-09-06'
status: draft
category: 自检
---

## 上标与下标

水的化学式是 H~2~O，勾股定理是 a^2^ + b^2^ = c^2^。

## 高亮

这里有一段 ==需要高亮的文字==，其余保持正常。

## 提示块

> [!TIP] 这是带标题的提示
> 提示块内部的 **加粗** 与 `行内代码` 应当正常渲染，
> 并且列表也要正常：
> - 第一项
> - 第二项

> [!WARNING]
> 这是没有写标题的警告块，标题应回退为默认中文标签。

> [!DANGER] 危险
> 这里是危险提示。

## 代码块不应被误伤

```js
const a = 1; // x^2^ 与 ==mark== 在代码里必须保持原样
```

行内 `x^2^` 与 `==mark==` 同样保持原样。

## 表格

| 列A | 列B |
| --- | --- |
| 1 | 2 |
| 3 | 4 |

## 删除线（原生）

~~被删除的文字~~ 与 **加粗** 并存。
