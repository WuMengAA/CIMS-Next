# 本工程产物要拷到 U 盘、在 Windows 目标机上直接双击运行，行尾必须钉死。
# .ps1 含 UTF-8 BOM，是 PowerShell 5.1 正确识别中文的前提，不要折腾编码。
*.ps1   text eol=crlf
*.cmd   text eol=crlf
*.md    text eol=lf
*.json  text eol=lf
*.html  text eol=lf
